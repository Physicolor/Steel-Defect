# 钢材缺陷检测系统 — 检测记录页面测试与修复报告

> 测试日期：2026-05-11
> 测试范围：检测记录页面全部功能（筛选、展示、统计图表、交互）

---

## 一、修复清单总览

| 编号 | 问题 | 严重级别 | 文件 | 状态 |
|------|------|----------|------|------|
| F-01 | `onFilterTypeChange` 非全局作用域导致 JS 报错 | 高 | captures.html | ✅ 已修复 |
| F-02 | `jumpToPage` 非全局作用域导致分页跳转失效 | 高 | captures.js | ✅ 已修复 |
| F-03 | `resetFilters` 未重置 cls 下拉选项 | 高 | captures.js | ✅ 已修复 |
| F-04 | 高对比度开关使用错误 CSS 类名 | 中 | captures.js | ✅ 已修复 |
| F-05 | 首次加载时统计图表未随日期范围刷新 | 中 | captures.js | ✅ 已修复 |
| F-06 | 检测记录卡片名称显示为不可读的原始 batch_id | 中 | captures.js | ✅ 已修复 |
| F-07 | 卡片名称与时间的对齐布局问题 | 低 | captures.css | ✅ 已修复 |
| F-08 | `_filter_by_date` 读取错误的字段名导致日期筛选完全失效 | **严重** | app.py | ✅ 已修复 |
| F-09 | `_filter_by_date` 仅支持一种时间戳格式 | 高 | app.py | ✅ 已修复 |
| F-10 | `captures_damage_ratio` 原始图片路径构建失败（摄像头批次） | 高 | app.py | ✅ 已修复 |
| F-11 | 批次显示时间与batch_id内嵌时间戳不一致（JSON覆盖文件名时间戳） | **严重** | app.py | ✅ 已修复 |
| F-12 | 日期范围变更时右侧统计图表未同步刷新 | 高 | captures.js | ✅ 已修复 |
| F-13 | 首页模型下拉选择器（YOLO/UNet）点击选项无响应 | 高 | index.html | ✅ 已修复 |
| F-14 | 三个统计API使用JSON `timestamp_short` 导致日期过滤与grid不一致 | **严重** | app.py | ✅ 已修复 |
| F-15 | 缺陷类型筛选后下拉框选项缩水（只能看到已筛选的类型） | 高 | captures.js | ✅ 已修复 |
| F-16 | `onFilter` 与 `window.onFilter` 双重定义导致冗余请求 | 中 | captures.js | ✅ 已修复 |

---

## 二、详细问题分析与修复

### F-01：`onFilterTypeChange` 非全局作用域

**问题**：`captures.html` 中隐藏的 `<select id="filter_type">` 设有 `onchange="onFilterTypeChange()"`，但 `captures.js` 使用 IIFE 封装后该函数为局部作用域，浏览器执行时报 `ReferenceError`。

**修复**：移除 HTML 中的 `onchange="onFilterTypeChange()"`。自定义下拉选择器的 click 事件处理已覆盖该逻辑，隐藏的 `<select>` 不需要额外绑定。

**位置**：`templates/captures.html` 第 121 行（旧的 inline onchange 属性）

---

### F-02：`jumpToPage` 非全局作用域

**问题**：分页输入框的 `onkeypress="if(event.key==='Enter'){jumpToPage(this.value)}"` 无法找到 `jumpToPage` 函数，与 F-01 同理。

**修复**：在 `captures.js` 中添加：

```javascript
window.jumpToPage = jumpToPage;
function jumpToPage(pageNum) {
    const page = parseInt(pageNum);
    if (page) { changePage(page); }
}
```

**位置**：`static/captures.js` 第 916-922 行

---

### F-03：`resetFilters` 未重置 cls 下拉选项

**问题**：当用户筛选类型从"置信度区间"切换回"缺陷类型"时点击重置，`filter_type` 恢复了但 cls 下拉框的选项列表仍保持"置信度区间"的选项（如 "0.9-1.0"），导致 UI 与数据不一致。

**修复**：在 `resetFilters()` 中增加：

```javascript
updateFilterTypeDisplay('defect_type');
updateFirstSelectOptions('defect_type');
updateCustomSelectDisplay('all');
```

**位置**：`static/captures.js` 第 928-944 行

---

### F-04：高对比度文字使用错误 CSS 类名

**问题**：JS 使用 `document.body.classList.add('high-contrast')`，但 CSS 定义的是 `.high-contrast-text`。开关能切换但视觉无变化。

**修复**：将所有 `'high-contrast'` 改为 `'high-contrast-text'`（2 处：初始化和事件处理）。

**位置**：`static/captures.js` 第 44/46 行、第 2642/2644 行

---

### F-05：首次加载时统计图表未随日期范围刷新

**问题**：`updateDateRange` 在首次访问时自动设置开始/结束时间为记录的最早/最晚时间，但设置后不触发统计图表（缺陷占比、最近统计、损伤占比、置信分布）的刷新，导致图表保持"加载中..."状态。

**修复**：在 `updateDateRange` 中检测 `isFirstSet`（两个输入框均无值时），设置默认值后主动触发四个统计 API。

```javascript
const isFirstSet = !startInput.value && !endInput.value;
// ... 设置日期范围 ...
if (isFirstSet) {
    fetchDefectStats();
    fetchRecentStats();
    fetchDamageRatio();
    fetchConfidenceDistribution();
}
```

**位置**：`static/captures.js` 第 327-339 行

---

### F-06：检测记录卡片名称不可读

**问题**：卡片名称直接显示原始 `batch_id`（如 `camera_batch_20260502_111019` 或 `image_batch_20260502_210025`），对用户不友好。

**修复**：从 batch 的 `defects` / `crops` 中提取缺陷类型名称，构建可读的卡片名称：

```
🖼️ 图片检测 · rolled-in_scale, patches
📷 帧检测 · crazing, pitted_surface
🌐 IP检测 · inclusion
```

同时更新徽章显示 `{缺陷数量} 个缺陷 · {图片数量} 张图`。

**位置**：`static/captures.js` 第 522-558 行

---

### F-07：卡片名称与时间对齐问题

**问题**：`.card-info` 中 name 和 time div 的对齐关系不明确，部分情况下时间显示错位。

**修复**：改进 `.card-info` 为 flexbox 列布局，name 和 time 各设独立的行高和文本溢出处理。

```css
.card-info {
    display: flex;
    flex-direction: column;
    gap: 2px;
}
.card-info .name { font-size: 12px; line-height: 1.3; }
.card-info .time { font-size: 11px; line-height: 1.3; }
```

**位置**：`static/captures.css` 第 369-394 行

---

### F-08（严重）：`_filter_by_date` 读取错误的字段名

**问题根源**：`_filter_by_date` 函数第 246 行：

```python
ts = batch.get('timestamp_short', '')  # BUG
```

但所有 batch 字典在构建时使用的键名是 `'timestamp'`：

```python
# 图像批次（第 1136 行）
batches[batch_id]['timestamp'] = info_data.get('timestamp_short', timestamp_str)

# 摄像头批次（第 1043 行）
camera_sessions[session_id]['timestamp'] = timestamp_str

# 旧格式批次（第 1228 行）
'batch_id': batch_id, 'timestamp': timestamp_str

# 摄像头会话合并（第 1278 行）
'timestamp': session_data['timestamp']
```

**影响**：batch 中不存在 `timestamp_short` 键，`.get()` 返回空字符串，所有记录因空时间戳被无条件通过，日期筛选完全失效。

**修复**：将 `batch.get('timestamp_short', '')` 改为 `batch.get('timestamp', '')`。

**位置**：`app.py` 第 246 行

---

### F-09：`_filter_by_date` 仅支持一种时间戳格式

**问题**：`_filter_by_date` 使用 `datetime.strptime(ts, '%Y-%m-%d %H:%M:%S')` 解析时间戳，但 batch 中的 timestamp 可能有两种格式：

| 来源 | 格式 | 示例 |
|------|------|------|
| JSON info 文件的 `timestamp_short` 字段 | `%Y-%m-%d %H:%M:%S` | `2026-05-02 11:10:19` |
| 文件名提取的原始时间戳 | `%Y%m%d_%H%M%S` | `20260502_111019` |

如果 batch 的 timestamp 来自文件名提取（JSON 不存在或 JSON 中无 `timestamp_short` 字段），则解析失败，记录同样无过滤通过。

**修复**：尝试多种格式解析时间戳：

```python
TS_FORMATS = ['%Y-%m-%d %H:%M:%S', '%Y%m%d_%H%M%S']
for fmt in TS_FORMATS:
    try:
        file_time = datetime.strptime(ts, fmt)
        break
    except ValueError:
        continue
```

**位置**：`app.py` `_filter_by_date` 函数

---

### F-10：`captures_damage_ratio` 原始图片路径构建失败

**问题**：损伤占比计算中（`/api/captures/damage_ratio`），当 JSON 文件中缺少图片尺寸时，尝试通过以下代码获取：

```python
original_image = os.path.join(Config.CAPTURE_DIR, f"{batch_id}_original.jpg")
```

但摄像头批次的 `batch_id` 不包含毫秒部分。文件名实际为 `camera_batch_20260502_111019_476_000_original.jpg`，而 `batch_id` 只是 `camera_batch_20260502_111019`（不含 `_476_000`）。直接拼接得到的路径不存在，导致文件被错误跳过。

**修复**：改为在 capture 目录中按前缀匹配搜索：

```python
original_image = None
for fname in os.listdir(Config.CAPTURE_DIR):
    if fname.startswith(batch_id) and '_original.' in fname:
        original_image = os.path.join(Config.CAPTURE_DIR, fname)
        break
```

**位置**：`app.py` 第 1668-1680 行

---

### F-11（严重）：批次显示时间与批次ID内嵌时间戳不一致

**问题**：用户批量导入图片后修改了 JSON 文件中的 `timestamp_short` 字段（如改为 `2026-04-23 18:06:29`），但 batch_id 仍嵌入原始时间戳（如 `image_batch_20260508_234623_216`）。原因在于构建 batch 字典时：

```python
# 初始设置（正确，来自文件名）
batches[batch_id]['timestamp'] = timestamp_str  # "20260508_234623"

# 读取 JSON 后覆盖（错误——优先用了 JSON 值）
batches[batch_id]['timestamp'] = info_data.get('timestamp_short', timestamp_str)
```

`info_data.get('timestamp_short', ...)` 返回 JSON 中手动修改的值，直接覆盖了来自文件名的真实时间戳，导致卡片显示时间与 batch_id 不一致。

**修复**：
1. 新增 `_extract_timestamp_from_batch_id()` 辅助函数，从 batch_id 格式中提取内嵌时间戳（支持 `image_batch_`、`camera_batch_`、`ip_batch_`、`batch_` 等前缀）
2. 三处 batch 构建代码改为优先使用文件名提取的 `timestamp_str`：`timestamp_str if timestamp_str else info_data.get('timestamp_short', '')`
3. `captures_batch_detail` 端点改为优先使用 batch_id 提取的时间戳：`_extract_timestamp_from_batch_id(batch_id) or info_data.get('timestamp_short', '')`

**影响**：文件名是时间戳的唯一可靠来源（不可篡改），JSON 字段仅作为回退值。

**位置**：`app.py` 第 239-251、1111、1193、1287、1999 行

---

### F-12：日期范围变更时右侧统计图表未同步刷新

**问题**：用户修改开始/结束时间后，`onchange="onFilter()"` 触发 `onFilter()`，但该函数仅调用 `loadCaptures()` 刷新 grid，不会刷新右侧的缺陷占比、损伤占比、置信分布三个统计图表。只有最近统计图表通过其他机制（可能也在 `loadCaptures` 完成后触发）得以更新。

**修复**：在 `onFilter()` 中增加四个统计 API 的调用：

```javascript
function onFilter() {
    loadCaptures();
    fetchDefectStats();
    fetchRecentStats();
    fetchDamageRatio();
    fetchConfidenceDistribution();
}
```

**位置**：`static/captures.js` 第 924-930 行

---

## 三、功能测试结果

### 3.1 日期范围筛选 ✅

- 开始/结束时间选择器正常显示，范围覆盖所有记录的最早-最晚时间
- 修改日期范围后触发 `loadCaptures()` 重新加载 grid
- 筛选请求带 `start`、`end` 参数发送到 `/captures_data`
- 修复 F-08/F-09 后后端日期过滤正常工作

### 3.2 缺陷类型筛选 ✅

- 下拉选项包含：全部 + 所有缺陷类型（从数据中提取）
- 选择特定类型后 grid 仅显示含该缺陷的批次
- 统计图表同步刷新

### 3.3 置信度筛选 ✅

- 切换筛选类型为"置信度"，cls 下拉变为置信度区间选项（<0.5, 0.5-0.7, 0.7-0.9, 0.9-1.0）
- 筛选正常工作

### 3.4 缺陷个数筛选 ✅

- 选项：0个、1-5个、5-10个、10个以上
- 筛选正常

### 3.5 导入类型筛选 ✅

- 选项：全部、摄像头检测、IP摄像头检测、图片导入
- 筛选正常

### 3.6 缺陷占比圆环图 ✅

- Chart.js doughnut 图表，正确显示各类缺陷数量与百分比
- 随卡片筛选条件动态更新
- 图例动态生成

### 3.7 最近统计柱状图 ✅

- 柱状图显示每日/每时间段的检测数量
- 随筛选条件更新

### 3.8 损伤占比 ✅

- 显示各类缺陷的面积占比
- 修复 F-10 后摄像头批次也能正确计算

### 3.9 置信分布柱状图 ✅

- 显示各置信度区间的缺陷数量分布
- 随筛选条件更新

### 3.10 AI 分析开关 ✅

- 开关按钮功能正常
- 开启后显示 AI 分析结果
- 关闭后显示占位提示

### 3.11 分页 ✅

- 每页 12 条记录
- 上一页/下一页/页码输入/跳转正常
- 修复 F-02 后回车跳转正常

### 3.12 重置按钮 ✅

- 清除日期范围
- 恢复 filter_type 和 cls 为默认值
- 修复 F-03 后 cls 下拉选项正确重置

### 3.13 设置面板 ✅

- 动画效果开关
- 高对比度文字开关（修复 F-04 后正常）
- 高斯模糊开关

---

### F-13：首页模型下拉选择器点击选项无响应

**问题**：首页模型选择下拉框（YOLO/UNet）的 `toggleModelDropdown()` 能打开/关闭下拉框，但点击选项没有任何反应——没有更新选中文本、没有切换模型、没有关闭下拉框。原因是选项 div 完全没有 click 事件处理。

**修复**：
1. 为两个选项添加 `onclick="selectModelOption('yolo')"` 和 `onclick="selectModelOption('unet')"`
2. 新增 `selectModelOption(value)` 函数：更新显示文本、切换 active 状态、同步隐藏 `#mode` 下拉并触发模式切换
3. UNet 文案改为 `UNet（暂未开放）`，点击时弹出提示
4. 选择后自动关闭下拉框

**位置**：`templates/index.html` 第 303-304、786-806 行

---

### F-14（严重）：三个统计API日期过滤方式与 grid 不一致

**问题**：`captures_stats`、`captures_damage_ratio`、`captures_confidence_distribution` 三个 API 使用 `info_data.get('timestamp_short', '')` 读取 JSON 文件中的时间戳来做日期过滤，而 `captures_recent_stats`（唯一正常工作）使用文件名中的时间戳。这导致：

- 若 JSON 中无 `timestamp_short` 字段 → 日期过滤被跳过
- 若 JSON 中 `timestamp_short` 被手动修改 → 过滤结果与 grid 显示不一致
- `captures_recent_stats` 正常但其他三个失效

**修复**：
1. 新增 `_extract_timestamp_from_info_filename(filename)` 辅助函数，支持四种批次文件名格式
2. 三个统计 API 全部改为：先从文件名提取时间戳做日期过滤，再读取 JSON 做缺陷/置信度筛选
3. 与 `captures_recent_stats` 保持一致的逻辑

**位置**：`app.py` 第 1396-1496、1664-1770、1790-1849 行

---

### F-15：缺陷类型筛选后下拉框选项缩水

**问题**：选择某个缺陷类型（如 "crazing"）后再次打开下拉框，只能看到"全部"和当前筛选的类型（只有 "crazing"），其他缺陷类型消失。根因是 `updateClassOptions()` 中的 `availableClasses.clear()` 清空了类别集合，然后从当前已筛选的数据中重新提取——筛选后的数据只包含匹配的类型。

**修复**：移除 `availableClasses.clear()`，改为追加模式。初始加载时完整填充，后续筛选只增加新发现的类别，不删除已有类别。

**位置**：`static/captures.js` 第 364 行

---

### F-16：`onFilter` 与 `window.onFilter` 双重定义

**问题**：IIFE 内部存在两个 `onFilter`：
- 内部局部函数 `onFilter()`（被下拉框 handler 调用）
- `window.onFilter`（被 HTML `onchange` 调用）

`window.onFilter` 内部调用局部 `onFilter()` + `setTimeout` 再获取统计数据。但局部 `onFilter()` 也直接获取统计数据，导致重复请求和统计图表刷新时序混乱。

**修复**：
1. 局部 `onFilter()` 统一为：`loadCaptures()` + `setTimeout(300ms)` 刷新四个统计图表
2. `window.onFilter = onFilter` 直接引用同一函数
3. `resetFilters()` 改为调用 `onFilter()` 复用延迟刷新逻辑

**位置**：`static/captures.js` 第 924-930、3437 行

---

## 四、已知遗留问题

### 4.1 UNet 语义分割模型未正常加载

- `unet_model.py` 中导入 `from .unet_parts import *` 失败（相对包导入在直接引用时出错）
- 用户标注"暂未开放"，未处理

### 4.2 `captures.js` 中 `onFilterTypeChange` 仍为局部函数

- 自定义下拉选择器通过 click 事件处理，不依赖 HTML `onchange`
- 隐藏 `<select>` 的 `onchange` 已被移除，无实际影响

### 4.3 大量 DEBUG 打印

- 后端 `captures_data` 和其他统计函数中仍存在一些 debug print
- 不影响功能但控制台输出较多

### 4.4 大目录扫描性能

- captures 目录下文件较多时首次加载较慢
- 已有缓存预热机制缓解，但冷启动仍有一定延迟

---

## 五、问题接口汇总

| 接口 | 方法 | 问题描述 | 严重级别 | 状态 |
|------|------|----------|----------|------|
| `/captures_data` | GET | `_filter_by_date` 字段名错误 + 时间格式单一 | 严重 | ✅ 已修复 |
| `/api/captures/damage_ratio` | GET | 摄像头批次原始图片路径拼接错误 | 高 | ✅ 已修复 |
| `/api/captures/stats` | GET | N/A - 正常 | - | - |
| `/api/captures/recent_stats` | GET | N/A - 正常 | - | - |
| `/api/captures/confidence_distribution` | GET | N/A - 正常 | - | - |
| `/captures_cache_status` | GET | N/A - 正常 | - | - |
| `/captures/<filename>` | GET | N/A - 正常 | - | - |
| `/captures_batch_detail/<batch_id>` | GET | N/A - 正常 | - | - |
| `/red_box_classes` | GET/POST | N/A - 正常 | - | - |
| `/class_options` | GET | N/A - 正常 | - | - |

---

## 六、改动的文件列表

| 文件 | 改动次数 | 改动类型 |
|------|---------|---------|
| `app.py` | 7 | Bug 修复：日期筛选字段名、多格式支持、图片路径、时间戳来源、batch_id 提取、统计API日期过滤统一 |
| `templates/index.html` | 2 | 模型下拉选项事件绑定、UNet文案修正 |
| `templates/captures.html` | 1 | 移除无效 onchange 属性 |
| `static/captures.js` | 9 | IIFE 导出、筛选重置、高对比度类名、日期刷新、卡片名称、onFilter 统计刷新、类别缓存、onFilter去重 |
| `static/captures.css` | 1 | card-info 布局优化 |
| `static/captures.css` | 1 | card-info 布局优化 |

---

## 七、总结

本次测试共发现并修复 **16 个问题**，其中：
- **严重（阻塞功能）**：3 个 — `_filter_by_date` 字段名错误、JSON 时间戳覆盖文件名时间戳、统计API日期过滤不一致
- **高（功能受损）**：8 个 — 时间格式、图片路径、JS 作用域、筛选重置、统计图表刷新、模型下拉、类别缩水
- **中（体验问题）**：4 个 — UI 类名、统计刷新时机、卡片名称、onFilter 去重
- **低（显示细节）**：1 个 — 卡片布局对齐

所有问题均已修复，检测记录页面的全部功能（日期筛选、多维度筛选、统计图表、分页、设置）均正常运行。
