# 钢材缺陷检测系统

## 一、系统概述

钢材缺陷检测系统是一个基于深度学习的实时钢材表面缺陷检测平台，采用YOLO目标检测和UNet语义分割双模架构，结合讯飞星火大模型AI分析能力，实现对钢材表面缺陷的智能化检测、分析和记录管理。

### 核心特点

- **双模检测**：支持YOLOv8目标检测和Channel-UNet语义分割两种检测模式
- **实时视频流**：支持USB摄像头和IP摄像头的实时检测
- **AI智能分析**：集成讯飞星火大模型，提供缺陷智能分析报告
- **自动记录**：检测结果自动保存，包含原图、标注图、热力图和裁剪图
- **邮件报警**：检测到缺陷时自动发送邮件通知
- **响应式界面**：适配桌面和移动端设备

---

## 二、代码结构图

```
SteelDefect/
│
├── app.py                          # Flask主应用入口（2264行）
│   ├── Config类                    # 系统配置管理
│   ├── SystemState类               # 线程安全的状态管理
│   ├── ModelService初始化          # 模型服务初始化
│   ├── RecordService初始化         # 记录服务初始化
│   ├── SparkImageService初始化     # AI分析服务初始化
│   ├── WebSocket路由               # 实时视频流传输
│   ├── REST API路由                # HTTP接口
│   └── 前端页面路由                # HTML模板渲染
│
├── services/                       # 服务层模块
│   ├── model_service.py           # 模型加载与推理服务（185行）
│   │   ├── ModelService类         # 统一模型服务
│   │   ├── YOLO推理               # 目标检测推理
│   │   ├── UNet推理               # 语义分割推理
│   │   └── 设备管理               # GPU/CPU自动切换
│   │
│   ├── video_service.py           # 摄像头管理服务（约70行）
│   │   ├── open_camera()          # 打开摄像头
│   │   └── 视频帧处理             # 帧读取和管理
│   │
│   ├── record_service.py          # 检测记录服务（约250行）
│   │   ├── RecordService类        # 记录管理服务
│   │   ├── save_record()          # 保存检测记录
│   │   ├── load_records()         # 加载历史记录
│   │   └── 缓存管理               # 性能优化缓存
│   │
│   ├── spark_image_service.py     # 讯飞图片理解服务（约280行）
│   │   ├── SparkImageService类    # 图片分析服务
│   │   ├── analyze_image()        # 图片缺陷分析
│   │   └── WebSocket通信          # 讯飞API通信
│   │
│   └── spark_lite_service.py      # 讯飞文本对话服务（约270行）
│       ├── SparkLiteService类     # 文本对话服务
│       └── chat_completion()      # 文本对话接口
│
├── segmentation/unet/              # UNet分割模型定义
│   ├── channel_unet_models.py     # Channel-UNet模型（约300行）
│   │   ├── ChannelUNet类          # 通道注意力UNet
│   │   ├── 编码器                 # 特征提取
│   │   ├── 解码器                 # 特征恢复
│   │   └── 注意力机制             # 通道注意力模块
│   │
│   └── unet_model.py              # 标准UNet模型（约50行）
│       └── UNet类                 # 基础UNet实现
│
├── templates/                      # HTML模板
│   ├── index.html                 # 主页（系统入口）
│   ├── detect.html                # 检测页面（核心功能）
│   └── captures.html              # 检测记录页面
│
├── static/                         # 静态资源
│   ├── style.css                  # 全局样式
│   ├── script.js                  # JavaScript脚本
│   ├── images/                    # 图片资源
│   └── fonts/                     # 字体文件
│
├── frontend/                       # React前端项目（可选）
│   ├── src/                       # React源代码
│   ├── package.json               # Node.js依赖
│   └── vite.config.ts             # Vite构建配置
│
├── captures/                       # 检测截图目录（运行时生成）
│   ├── *_original.jpg             # 原始图片
│   ├── *_annotated.jpg            # 标注图片
│   ├── *_heatmap.jpg              # 热力图
│   ├── *_crop_*.jpg               # 缺陷裁剪图
│   └── *_info.json                # 批次信息
│
├── uploads/                        # 上传文件目录
│
├── captures特殊/                   # 特殊截图目录
│
├── .env                            # 环境变量配置（敏感信息）
├── .env.example                    # 环境变量示例
├── .gitignore                      # Git忽略配置
├── requirements.txt                # Python依赖列表
├── model_config.json               # 模型配置文件
├── records.json                    # 检测记录数据库
├── start.bat                       # Windows启动脚本
├── start_conda.bat                 # Conda环境启动脚本
├── best (1).pt                     # YOLO检测模型（5.3MB）
└── myChannelUnet_2_neudet_best.pth # UNet分割模型（192MB）
```

---

## 三、系统需求分析

### 3.1 功能需求

#### 3.1.1 核心检测功能
1. **双模检测支持**
   - YOLOv8目标检测：识别缺陷类型和位置
   - Channel-UNet语义分割：像素级缺陷区域分割
   - 实时切换检测模式

2. **实时视频流处理**
   - 支持USB摄像头（索引0, 1, 2...）
   - 支持IP摄像头（RTSP/HTTP协议）
   - 帧率自适应调整
   - WebSocket实时传输

3. **缺陷识别类别**
   - 划痕（scratches）
   - 夹杂（inclusion）
   - 裂纹（crazing）
   - 压入氧化铁皮（rolled-in scale）
   - 麻点（pitted surface）
   - 斑块（patches）

#### 3.1.2 数据管理功能
1. **检测记录保存**
   - 自动保存检测批次
   - 每批次包含：原图、标注图、热力图、裁剪图、JSON信息
   - 时间戳命名避免冲突

2. **历史记录查询**
   - 按日期范围筛选
   - 分页显示
   - 缩略图预览
   - 详情查看

3. **数据缓存优化**
   - 检测记录缓存（TTL 30秒）
   - 减少文件系统I/O
   - 提升页面加载速度

#### 3.1.3 AI分析功能
1. **图片智能分析**
   - 调用讯飞星火图片理解API
   - 分析缺陷成因和影响
   - 生成专业检测报告

2. **文本对话交互**
   - 讯飞星火Lite模型对话
   - 缺陷知识问答
   - 工艺改进建议

#### 3.1.4 报警通知功能
1. **邮件报警**
   - SMTP协议发送邮件
   - 检测到缺陷时触发
   - 包含缺陷图片和分析结果

2. **可配置警戒区域**
   - 设置警戒线
   - 设置警戒区域
   - 区域内缺陷优先报警

#### 3.1.5 参数调节功能
1. **检测参数调整**
   - 置信度阈值（0.0-1.0）
   - IOU阈值（0.0-1.0）
   - 截图间隔时间

2. **模型管理**
   - 动态加载模型
   - 模型历史记录
   - 最近使用模型快速切换

### 3.2 性能需求

1. **实时性要求**
   - 视频流延迟 < 200ms
   - 单帧推理时间 < 100ms（GPU）
   - 帧率 ≥ 15 FPS（640x640分辨率）

2. **响应时间**
   - 页面加载时间 < 2s
   - 检测记录加载 < 1s（有缓存）
   - AI分析响应 < 10s

3. **资源占用**
   - GPU显存占用 < 4GB
   - CPU占用率 < 80%
   - 内存占用 < 2GB

4. **并发支持**
   - 支持单用户多标签页访问
   - WebSocket连接稳定
   - 线程安全的状态管理

### 3.3 安全需求

1. **API密钥管理**
   - 讯飞API密钥存储在.env文件
   - 不提交到版本控制
   - 前端不暴露密钥

2. **数据隐私**
   - 检测记录本地存储
   - 图片数据不上传云端（除AI分析外）
   - records.json定期备份

3. **访问控制**
   - 本地部署，内网访问
   - 可扩展用户认证
   - WebSocket连接验证

---

## 四、系统架构图

### 4.1 技术栈说明

#### 后端技术
- **Web框架**：Flask 2.3+
- **深度学习**：PyTorch 2.0+、Ultralytics YOLO 8.0+
- **图像处理**：OpenCV 4.8+、Pillow 9.5+、NumPy 1.24+
- **实时通信**：WebSocket（Flask-SocketIO）
- **邮件服务**：smtplib
- **环境管理**：python-dotenv

#### 前端技术
- **基础**：HTML5、CSS3、JavaScript ES6+
- **可视化**：Canvas API
- **图表**：Chart.js（统计分析）
- **可选**：React + TypeScript + Vite（frontend目录）

#### AI模型
- **目标检测**：YOLOv8（预训练模型微调）
- **语义分割**：Channel-UNet（自定义注意力机制）
- **大模型**：讯飞星火4.0Ultra（图片理解）、Spark Lite（文本对话）

#### 开发工具
- **IDE**：VS Code
- **版本控制**：Git
- **包管理**：pip、npm

### 4.2 模块划分

```
┌─────────────────────────────────────────────────────────┐
│                     用户界面层                           │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐              │
│  │ index    │  │ detect   │  │ captures │              │
│  │ .html    │  │ .html    │  │ .html    │              │
│  └──────────┘  └──────────┘  └──────────┘              │
│       ↓              ↓              ↓                   │
│  ┌──────────────────────────────────────────┐           │
│  │      static/style.css + script.js        │           │
│  └──────────────────────────────────────────┘           │
└─────────────────────────────────────────────────────────┘
                          ↓
┌─────────────────────────────────────────────────────────┐
│                   Web服务层 (Flask)                      │
│  ┌────────────────────────────────────────────┐         │
│  │  app.py (2264行)                           │         │
│  │  - REST API路由                            │         │
│  │  - WebSocket路由                           │         │
│  │  - 模板渲染                                │         │
│  │  - 状态管理 (SystemState)                  │         │
│  └────────────────────────────────────────────┘         │
└─────────────────────────────────────────────────────────┘
                          ↓
┌─────────────────────────────────────────────────────────┐
│                   业务服务层                             │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐  │
│  │ ModelService │  │VideoService  │  │RecordService │  │
│  │              │  │              │  │              │  │
│  │ - YOLO推理   │  │ - 摄像头管理 │  │ - 记录保存   │  │
│  │ - UNet推理   │  │ - 帧读取     │  │ - 记录查询   │  │
│  │ - 设备管理   │  │ - 会话管理   │  │ - 缓存管理   │  │
│  └──────────────┘  └──────────────┘  └──────────────┘  │
│  ┌──────────────────────┐  ┌──────────────────────┐    │
│  │SparkImageService     │  │SparkLiteService      │    │
│  │                      │  │                      │    │
│  │ - 图片理解API        │  │ - 文本对话API        │    │
│  │ - WebSocket通信      │  │ - 聊天完成           │    │
│  └──────────────────────┘  └──────────────────────┘    │
└─────────────────────────────────────────────────────────┘
                          ↓
┌─────────────────────────────────────────────────────────┐
│                   模型层                                 │
│  ┌──────────────────────┐  ┌──────────────────────┐    │
│  │ YOLOv8 (.pt)         │  │ Channel-UNet (.pth)  │    │
│  │ - 目标检测           │  │ - 语义分割           │    │
│  │ - 6个缺陷类别        │  │ - 通道注意力机制     │    │
│  └──────────────────────┘  └──────────────────────┘    │
└─────────────────────────────────────────────────────────┘
                          ↓
┌─────────────────────────────────────────────────────────┐
│                   数据存储层                             │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐              │
│  │captures/ │  │records.  │  │uploads/  │              │
│  │(图片)    │  │json      │  │(临时)    │              │
│  └──────────┘  └──────────┘  └──────────┘              │
└─────────────────────────────────────────────────────────┘
```

### 4.3 数据流向

#### 4.3.1 实时检测流程

```
摄像头 → VideoService → 视频帧 → ModelService → 检测结果
                                    ↓
                              SystemState更新
                                    ↓
                              WebSocket推送 → 前端Canvas绘制
                                    ↓
                              定时截图 → RecordService → captures/
```

#### 4.3.2 AI分析流程

```
用户选择图片 → 前端上传 → app.py接收
                                    ↓
                          SparkImageService
                                    ↓
                          WebSocket连接讯飞API
                                    ↓
                          发送图片Base64编码
                                    ↓
                          接收分析结果JSON
                                    ↓
                          返回前端展示报告
```

#### 4.3.3 记录查询流程

```
用户访问captures.html → 请求/captures_data
                                    ↓
                          RecordService检查缓存
                                    ↓
                    缓存命中 → 直接返回缓存数据
                    缓存未命中 → 扫描captures目录
                                    ↓
                          解析*_info.json文件
                                    ↓
                          更新缓存并返回数据
                                    ↓
                          前端渲染卡片列表
```

---

## 五、算法说明

### 5.1 YOLO目标检测算法

#### 5.1.1 算法原理

YOLOv8（You Only Look Once v8）是一种单阶段目标检测算法，将目标检测任务转化为回归问题，通过一次前向传播即可得到检测结果。

**核心思想**：
- 将输入图像划分为S×S的网格
- 每个网格预测B个边界框及其置信度
- 同时预测C个类别概率
- 输出维度：S×S×(B×5+C)

#### 5.1.2 网络架构

```
Input (640×640×3)
    ↓
Backbone (CSPDarknet)
    ↓
Neck (PANet)
    ↓
Head (Detection Head)
    ↓
Output (boxes, scores, classes)
```

**主要组件**：
1. **Backbone**：CSPDarknet53，提取特征
2. **Neck**：PANet（Path Aggregation Network），多尺度特征融合
3. **Head**：解耦头，分别预测分类和回归

#### 5.1.3 损失函数

```
Loss = λ_coord × L_coord + λ_conf × L_conf + λ_cls × L_cls

其中：
- L_coord：边界框回归损失（CIoU Loss）
- L_conf：置信度损失（BCE Loss）
- L_cls：分类损失（BCE Loss）
```

#### 5.1.4 在系统中的应用

**模型配置**：
- 输入尺寸：640×640
- 检测类别：6种钢材缺陷
- 置信度阈值：0.25（可调）
- IOU阈值：0.45（NMS过滤）

**推理流程**：
```python
# 伪代码
results = model(frame, conf=0.25, iou=0.45)
for box in results.boxes:
    x1, y1, x2, y2 = box.xyxy[0]
    confidence = box.conf[0]
    class_id = int(box.cls[0])
    class_name = model.names[class_id]
    # 绘制边界框和标签
```

**性能优化**：
- GPU加速（CUDA）
- 批量推理（batch_size=1）
- 半精度推理（FP16，可选）

### 5.2 UNet语义分割算法

#### 5.2.1 算法原理

UNet是一种编码器-解码器结构的语义分割网络，最初用于生物医学图像分割。其特点是U型对称结构和跳跃连接。

**核心思想**：
- 编码器：逐步下采样，提取高层次语义特征
- 解码器：逐步上采样，恢复空间分辨率
- 跳跃连接：将编码器的细节信息传递给解码器

#### 5.2.2 Channel-UNet改进

本系统使用Channel-UNet，在标准UNet基础上引入通道注意力机制。

**网络架构**：

```
Input (640×640×3)
    ↓
Encoder (4个阶段)
    Conv + BN + ReLU + MaxPool
    ↓
Bottleneck
    Conv + BN + ReLU
    ↓
Channel Attention Module
    Global Average Pooling
    FC + ReLU + FC + Sigmoid
    ↓
Decoder (4个阶段)
    UpSample + Concat + Conv + BN + ReLU
    ↓
Output (640×640×num_classes)
    ↓
Softmax → 像素级分类
```

**通道注意力机制**：
```python
class ChannelAttention(nn.Module):
    def __init__(self, channels, reduction=16):
        super().__init__()
        self.avg_pool = nn.AdaptiveAvgPool2d(1)
        self.fc = nn.Sequential(
            nn.Linear(channels, channels // reduction),
            nn.ReLU(),
            nn.Linear(channels // reduction, channels),
            nn.Sigmoid()
        )
    
    def forward(self, x):
        b, c, _, _ = x.size()
        y = self.avg_pool(x).view(b, c)
        y = self.fc(y).view(b, c, 1, 1)
        return x * y.expand_as(x)
```

#### 5.2.3 损失函数

```
Loss = CrossEntropyLoss(prediction, ground_truth)
     + DiceLoss(prediction, ground_truth)

其中：
- CrossEntropyLoss：处理类别不平衡
- DiceLoss：优化分割区域的交并比
```

#### 5.2.4 在系统中的应用

**模型配置**：
- 输入尺寸：640×640
- 输出通道：num_classes（缺陷类别数）
- 激活函数：Softmax（多分类）

**推理流程**：
```python
# 伪代码
input_tensor = preprocess(frame)  # 归一化、转为tensor
with torch.no_grad():
    output = model(input_tensor)
    prediction = torch.argmax(output, dim=1)
    heatmap = generate_heatmap(output)
    mask = create_mask(prediction)
```

**后处理**：
- 生成热力图：将softmax概率可视化
- 创建掩码：将预测结果转为二值图像
- 轮廓提取：cv2.findContours获取缺陷边界

### 5.3 讯飞星火AI分析

#### 5.3.1 图片理解API

**功能**：对钢材缺陷图片进行智能分析，生成专业检测报告。

**工作流程**：
```
1. 图片预处理
   - 调整尺寸（≤ 2048px）
   - Base64编码
   
2. 构建请求
   {
     "header": {
       "app_id": "xxx",
       "uid": "unique_id"
     },
     "parameter": {
       "chat": {
         "domain": "image",
         "temperature": 0.5,
         "max_tokens": 2048
       }
     },
     "payload": {
       "message": {
         "text": [
           {"role": "user", "content": "请分析这张钢材缺陷图片..."},
           {"role": "user", "content": "<image_base64>"}
         ]
       }
     }
   }
   
3. WebSocket通信
   - 建立加密连接（wss）
   - 鉴权URL生成（HMAC-SHA256）
   - 分块发送请求
   - 流式接收响应
   
4. 结果解析
   - JSON格式响应
   - 提取分析文本
   - 前端Markdown渲染
```

**提示词设计**：
```
你是一位专业的钢材质量检测工程师。请分析这张钢材表面图片：
1. 识别缺陷类型（划痕、夹杂、裂纹等）
2. 描述缺陷特征（位置、大小、形状）
3. 评估缺陷严重程度
4. 分析可能的成因
5. 给出处理建议

请用专业但易懂的语言回答，分点说明。
```

#### 5.3.2 文本对话API（Spark Lite）

**功能**：提供缺陷知识问答和工艺改进建议。

**工作流程**：
```
1. 用户输入问题
2. 构建对话历史
3. 调用Spark Lite API
4. 流式返回回答
5. 前端打字机效果展示
```

**应用场景**：
- 缺陷成因咨询
- 质量标准查询
- 工艺参数优化
- 检测方法建议

#### 5.3.3 安全机制

**鉴权方式**：
```python
import hmac
import hashlib
import base64
from urllib.parse import urlencode

def generate_auth_url(api_key, api_secret, host, path):
    # 生成时间戳
    timestamp = int(time.time())
    
    # 构建签名字符串
    signature_origin = f"host: {host}\ndate: {timestamp}\nGET {path} HTTP/1.1"
    
    # HMAC-SHA256签名
    signature_sha = hmac.new(
        api_secret.encode('utf-8'),
        signature_origin.encode('utf-8'),
        digestmod=hashlib.sha256
    ).digest()
    
    # Base64编码
    signature = base64.b64encode(signature_sha).decode('utf-8')
    
    # 构建授权头
    authorization = base64.b64encode(
        f'api_key="{api_key}", algorithm="hmac-sha256", headers="host date request-line", signature="{signature}"'.encode('utf-8')
    ).decode('utf-8')
    
    # 拼接URL
    query = urlencode({
        'authorization': authorization,
        'date': timestamp,
        'host': host
    })
    
    return f"wss://{host}{path}?{query}"
```

---

## 六、部署与运行说明

### 6.1 环境要求

**硬件要求**：
- CPU：Intel i5或AMD Ryzen 5及以上
- GPU：NVIDIA GTX 1060或以上（可选，推荐RTX 3060+）
- 内存：8GB RAM（推荐16GB）
- 存储：10GB可用空间（模型+记录）

**软件要求**：
- 操作系统：Windows 10/11、Linux、macOS
- Python：3.10或更高版本
- CUDA：11.7+（如需GPU加速）
- 浏览器：Chrome 90+、Edge 90+、Firefox 88+

### 6.2 安装步骤

#### 步骤1：克隆或下载项目
```bash
cd D:\Users\12404\Documents\钢材检测系统\SteelDefect
```

#### 步骤2：创建虚拟环境（推荐）
```bash
python -m venv venv
venv\Scripts\activate  # Windows
# source venv/bin/activate  # Linux/macOS
```

#### 步骤3：安装依赖
```bash
pip install -r requirements.txt
```

或使用国内镜像加速：
```bash
pip install -r requirements.txt -i https://pypi.tuna.tsinghua.edu.cn/simple
```

#### 步骤4：配置环境变量
复制`.env.example`为`.env`，填写配置：
```bash
# Flask配置
FLASK_DEBUG=false
FLASK_HOST=0.0.0.0
FLASK_PORT=5000

# 设备配置
USE_CUDA=true

# 邮件报警（可选）
SMTP_HOST=smtp.qq.com
SMTP_PORT=465
SMTP_USER=your_email@qq.com
SMTP_PASS=your_authorization_code
EMAIL_RECEIVER=receiver@example.com

# 讯飞星火API（AI分析必需）
SPARK_API_KEY=user_id:api_key
SPARK_MODEL=4.0Ultra

# 讯飞图片理解API
SPARK_IMAGE_APP_ID=your_app_id
SPARK_IMAGE_API_KEY=your_api_key
SPARK_IMAGE_API_SECRET=your_api_secret
```

#### 步骤5：准备模型文件
确保以下模型文件在项目根目录：
- `best (1).pt` - YOLO检测模型
- `myChannelUnet_2_neudet_best.pth` - UNet分割模型

#### 步骤6：启动系统

**方式1：使用启动脚本**
```bash
start.bat  # Windows普通启动
# 或
start_conda.bat  # Conda环境启动
```

**方式2：命令行启动**
```bash
python app.py
```

#### 步骤7：访问系统
打开浏览器访问：http://localhost:5000

### 6.3 使用说明

#### 基本操作流程

1. **选择摄像头源**
   - 本地摄像头：输入索引号（0=默认，1=外接）
   - IP摄像头：输入RTSP/HTTP URL

2. **加载模型**
   - 点击"选择模型"按钮
   - 选择YOLO或UNet模型文件
   - 等待模型加载完成

3. **调整检测参数**
   - 置信度阈值：0.0-1.0（越高越严格）
   - IOU阈值：0.0-1.0（控制重复框）
   - 截图间隔：自动保存间隔（秒）

4. **开始检测**
   - 点击"开始检测"按钮
   - 实时查看检测结果
   - 快捷键：Space（开始/暂停）、S（截图）、Esc（停止）

5. **查看记录**
   - 点击"检测记录"按钮
   - 浏览历史检测批次
   - 查看详情和AI分析报告

#### 高级功能

1. **AI智能分析**
   - 在检测记录页面选择批次
   - 点击"AI分析"按钮
   - 等待分析报告生成（约5-10秒）

2. **邮件报警配置**
   - 在系统设置中配置SMTP服务器
   - 设置接收邮箱地址
   - 启用报警开关

3. **警戒区域设置**
   - 在检测页面绘制警戒线
   - 设置警戒区域多边形
   - 区域内缺陷优先报警

### 6.4 常见问题

#### Q1: 如何提升检测速度？
**A**: 
- 使用NVIDIA GPU并安装CUDA版PyTorch
- 降低输入分辨率（修改IMG_SIZE参数）
- 提高置信度阈值减少检测框数量
- 关闭不必要的后台程序

#### Q2: 摄像头无法连接怎么办？
**A**:
- 检查摄像头是否被其他程序占用
- 尝试不同的摄像头索引（0, 1, 2...）
- 对于IP摄像头，确认URL格式正确且网络可达
- 检查防火墙设置

#### Q3: 如何更换检测模型？
**A**:
- 将新的.pt或.pth文件放到项目根目录
- 在网页界面选择新模型
- 或在model_config.json中修改current_model

#### Q4: 检测记录保存在哪里？
**A**:
- 图片保存在captures/目录
- 记录数据保存在records.json
- 每个批次包含原图、标注图、热力图和JSON信息

#### Q5: 如何启用邮件报警？
**A**:
- 配置SMTP服务器信息（推荐使用QQ邮箱）
- 获取授权码（不是登录密码）
- 在系统中设置接收邮箱地址
- 测试邮件发送功能

#### Q6: AI分析功能无法使用？
**A**:
- 检查.env文件中讯飞API配置是否正确
- 确认已安装python-dotenv：`pip install python-dotenv`
- 验证API密钥是否有效（访问讯飞开放平台）
- 查看控制台日志获取详细错误信息

### 6.5 维护与优化

#### 定期维护
1. **清理旧记录**
   ```bash
   # 手动删除captures/中的旧批次
   # 或编写脚本自动清理30天前的记录
   ```

2. **备份数据**
   ```bash
   # 备份records.json
   copy records.json records_backup_20260509.json
   
   # 备份重要截图
   zip captures_backup.zip captures\*
   ```

3. **更新模型**
   - 定期重新训练模型
   - 评估新模型性能
   - 灰度发布新模型

#### 性能监控
1. **系统资源监控**
   - GPU使用率：`nvidia-smi`
   - CPU使用率：任务管理器
   - 内存占用：资源监视器

2. **日志记录**
   - Flask控制台输出
   - 错误日志分析
   - 性能瓶颈定位

3. **缓存管理**
   - 监控缓存命中率
   - 调整缓存TTL
   - 定期清除无效缓存

---

## 七、总结

钢材缺陷检测系统是一个功能完善的工业级缺陷检测平台，具有以下优势：

1. **技术先进**：采用最新的YOLOv8和Channel-UNet算法，结合大模型AI分析
2. **性能优异**：GPU加速推理，实时视频流处理，缓存优化查询
3. **易于使用**：友好的Web界面，简单的配置流程，详细的文档说明
4. **可扩展性强**：模块化设计，支持自定义模型，易于添加新功能
5. **安全可靠**：本地部署，数据隐私保护，API密钥安全管理

本系统适用于钢铁生产企业的质量检测环节，可有效提高检测效率，降低人工成本，提升产品质量控制水平。

---

**文档版本**：v1.0  
**更新日期**：2026-05-09  
**维护者**：SteelDefect开发团队
